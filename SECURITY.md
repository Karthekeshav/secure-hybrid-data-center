# Security policy

This repository contains a classroom network-security simulation.

- Do not add real credentials, API keys, tokens, personal data, or institutional secrets.
- Use only obvious demonstration credentials inside the `.pkt` file.
- The configurations in `configurations/` intentionally contain credential placeholders.
- Packet Tracer controls shown here are educational representations and should not be copied directly into production.

